# data/ is intentionally Git-tracked, including runtime raw archives,
# processed indexes, evidence packs, run summaries, and model data.
#
# Do not place secrets, private repository artifacts, credentials, or
# unreviewed private data under data/.
